# Role & Context
You are an expert front-end engineer specializing in React, Vite, Tailwind CSS, and lucide-react.
I am building a Proof of Concept (PoC) for an internal agent workbench. 
I have a modern React app initialized in `./Frontend` (with react-router-dom and lucide-react already installed).

# UI Design & Layout (CRITICAL REFERENCE)
I have attached the design mockup image `Flow B - Stack view.jpg`. You MUST strictly replicate this layout, visual hierarchy, alignment, and spacing using Tailwind CSS. 
- Use a clean, professional **Dark Mode theme** (slate/zinc dark backgrounds, white or slightly lighter panels for contrast, crisp light borders).
- Ensure all text placeholders, form labels, and badge colors (e.g., green for MEMBER, yellow for NON-MEMBER) exactly match the image.

The page is structured as a vertical **Stack View** with 3 distinct functional sections:
1. **Top Section (Search Bar)**: A horizontal row labeled "Search members & non-members" containing input fields for First Name, Last Name, Email, Phone, Address, Membership #, alongside a blue "Search" button and a white "Clear" button.
2. **Middle Section (Customer Table)**: A data table with columns: NAME, TYPE, MEMBERSHIP #, LOCATION, PHONE. Rows should be cleanly separated and highlight upon hover.
3. **Bottom Section (Preference Detail Form)**: A comprehensive management panel containing:
   - A customer profile info header (Name, Member status, Contact info) and a red "Do Not Contact (all)" checkbox.
   - A Preference Matrix Table: Mapping Topic rows (e.g., "Allow contact globally", "Membership & Account", etc.) against 4 checkbox columns (CALL, EMAIL, SMS, MAIL).
   - Consent & opt-in audit log: A bulleted list with small green dots showing historical changes.
   - Action Footer: A blue "Save preferences" button and a white "Cancel" button.

# Stateful & Routing Logic
Please use `react-router-dom` to implement the state flow smoothly inside `./Frontend/src`:
- **Initial State**: Only the Top Section (Search Bar) is visible. The middle and bottom sections are hidden.
- **Search Triggered**: 
  - Mock an API call to fetch customer results.
  - If multiple customers are returned, render the Middle Section (Customer Table).
  - If exactly 1 customer is returned, skip the table and instantly reveal the Bottom Section (Preference Detail Form) for that customer.
- **Row Selection**: Clicking any row in the Middle Section instantly loads and displays that specific customer's data in the Bottom Section.
- **Save Event**: Clicking "Save preferences" should mock a successful update, displaying a beautiful, floating green Lucide-react alert/toast notification saying "Customer preferences updated successfully!".

Please analyze the `./Frontend` directory structure and let's start by generating the components, routing, and Tailwind styling now.